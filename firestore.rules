rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Rules for the public livestock collection
    match /livestock/{livestockId} {
      // Allow anyone to read and write to the livestock collection.
      // This makes the database fully public.
      allow read, write: if true;
    }
  }
}
